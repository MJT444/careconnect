### SQL folder

It contains the DDL of the postgressSQL table

### Observation

The query is functional and reasonably fast, but the plan contains sequential scans and disk-based sorts. Thus, it is acceptable performance-wise but not ideal.
